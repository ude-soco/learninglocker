apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: api
        image: learninglocker
        args:
        - node
        - api/dist/server
        envFrom:
        - configMapRef:
            name: config
        - secretRef:
            name: secret
        env:
        - name: PATH_PREFIX
          value: "/api"
        - name: REDIS_HOST
          value: $(REDIS_SERVICE)
        - name: MONGO_HOST
          value: $(MONGO_SERVICE)
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30