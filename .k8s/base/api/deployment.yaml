apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 1
  template:
    spec:
      initContainers:
      - name: migrations
        image: learninglocker
        args: [migrations]
        envFrom:
        - configMapRef:
            name: config
        - secretRef:
            name: secret
        env:
        - name: MONGO_HOST
          value: $(MONGO_SERVICE)

      - name: seeds
        image: learninglocker
        args: [seeds]
        envFrom:
        - configMapRef:
            name: config
        - secretRef:
            name: secret
        env:
        - name: MONGO_HOST
          value: $(MONGO_SERVICE)

      containers:
      - name: api
        image: learninglocker
        args: [api]
        envFrom:
        - configMapRef:
            name: config
        - secretRef:
            name: secret
        env:
        - name: PATH_PREFIX
          value: "/api"
        - name: REDIS_HOST
          value: $(REDIS_SERVICE)
        - name: MONGO_HOST
          value: $(MONGO_SERVICE)
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30
