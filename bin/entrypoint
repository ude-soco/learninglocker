#!/usr/bin/env sh
set -eu

MONGO_HOST="${MONGO_HOST:?MONGO_HOST missing}"
MONGO_PORT="${MONGO_PORT:-27017}"
MONGO_DATABASE="${MONGO_DATABASE:?MONGO_DATABASE missing}"
export MONGODB_PATH="mongodb://${MONGO_HOST}:${MONGO_PORT}/${MONGO_DATABASE}"

export PATH=./bin:$PATH
exec "$@"
